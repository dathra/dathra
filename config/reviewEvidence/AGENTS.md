# reviewEvidence - AI Agent Instructions

Read `SPEC.typ` and `implementation.test.mjs` before changing the CLI.

The command is review-process infrastructure. Keep its output deterministic,
avoid shell evaluation, validate all repository-relative paths, and preserve a
non-zero exit for every evidence mismatch. Code comments and JSDoc must be in
English.
